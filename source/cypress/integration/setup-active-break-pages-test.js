describe('setup-active-break-pages Tests', () => {
    beforeEach(() => {
        cy.visit('http://127.0.0.1:5500/source/HTML/setup-active-break-pages.html');
    });
    it("sample test", ()=>{
        expect(true).to.equal(true);
    });
});