describe('setup-active-break-pages Tests', () => {
    beforeEach(() => {
        cy.visit('http://localhost:8080/HTML/setup-active-break-pages.html');
    });
    it("sample test", ()=>{
        expect(true).to.equal(true);
    });
});