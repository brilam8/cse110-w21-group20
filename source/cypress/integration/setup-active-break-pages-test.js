describe('setup-active-break-pages Tests', () => {
    beforeEach(() => {
        cy.visit('http://127.0.0.1:8080/HTML/setup-active-break-pages.html');
    });
    it("sample test", ()=>{
        expect(true).to.equal(true);
    });
});